/**
 * Metadata specific to interaction blocks
 */
export interface InteractionMetadata {
    // Add properties specific to interactions here
    title?: string;
    description?: string;
    timestamp?: string;
} 